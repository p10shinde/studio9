import { AddressTransformPipe } from './address-transform.pipe';

describe('AddressTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new AddressTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
