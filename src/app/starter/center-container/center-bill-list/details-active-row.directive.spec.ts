import { DetailsActiveRowDirective } from './details-active-row.directive';

describe('DetailsActiveRowDirective', () => {
  it('should create an instance', () => {
    const directive = new DetailsActiveRowDirective();
    expect(directive).toBeTruthy();
  });
});
